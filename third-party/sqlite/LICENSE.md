# SQLite public-domain notice

SQLite version 3.44.2 is included as the official amalgamation in
`sqlite3.c`, `sqlite3.h` and `sqlite3ext.h`.

The author disclaims copyright to this source code. In place of a legal
notice, the source carries this blessing:

> May you do good and not evil.  
> May you find forgiveness for yourself and forgive others.  
> May you share freely, never taking more than you give.

The full notice is embedded near the beginning of both `sqlite3.c` and
`sqlite3.h`. SQLite is dedicated to the public domain.
